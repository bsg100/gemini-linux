diff --git a/drivers/base/regmap/regmap-debugfs.c b/drivers/base/regmap/regmap-debugfs.c
index f36027591..901f9b830 100644
--- a/drivers/base/regmap/regmap-debugfs.c
+++ b/drivers/base/regmap/regmap-debugfs.c
@@ -290,7 +290,9 @@ static ssize_t regmap_map_read_file(struct file *file, char __user *user_buf,
 				   count, ppos);
 }
 
-#undef REGMAP_ALLOW_WRITE_DEBUGFS
+/* GEMINI-DEBUG (build #268): writable regmap debugfs for live MT6351
+ * codec/PMIC register experiments over pwrap. Never ship enabled. */
+#define REGMAP_ALLOW_WRITE_DEBUGFS
 #ifdef REGMAP_ALLOW_WRITE_DEBUGFS
 /*
  * This can be dangerous especially when we have clients such as
@@ -689,6 +691,7 @@ void regmap_debugfs_exit(struct regmap *map)
 
 void regmap_debugfs_initcall(void)
 {
+	pr_info("GEMINI-DEBUG: regmap debugfs writes enabled\n");
 	struct regmap_debugfs_node *node, *tmp;
 
 	regmap_debugfs_root = debugfs_create_dir("regmap", NULL);
